# Copy to .env.local and fill in your own TMDB credentials.
# Get them at https://www.themoviedb.org/settings/api

# TMDB v4 Read Access Token (preferred — used as a Bearer token server-side).
TMDB_ACCESS_TOKEN=

# TMDB v3 API key (optional fallback if no access token is set).
TMDB_API_KEY=
